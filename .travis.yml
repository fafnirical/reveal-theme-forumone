language: node_js
node_js:
  - '6'
  - '4'

script: npm run build

deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN # Set in travis-ci.org dashboard
  on:
    branch: travis-ci-build
  target_branch: travis-ci-build-target
  local_dir: dist
